#ifndef OBSTACLE_H
#define OBSTACLE_H
#include "Element.h"

class Obstacle : public Element
{
    public:
        Obstacle(Position);
        virtual ~Obstacle();

    protected:

    private:

};

#endif // OBSTACLE_H
